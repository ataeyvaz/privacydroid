package io.privacydroid.ui.theme

import androidx.compose.ui.graphics.Color

// --- Koyu tema ana renkleri ---
val DarkBackground = Color(0xFF0D0D0D)
val DarkSurface = Color(0xFF1A1A1A)
val DarkSurfaceVariant = Color(0xFF242424)
val DarkOutline = Color(0xFF3A3A3A)

// Birincil renk: Yeşil (güvenlik / gözetleme teması)
val PrimaryGreen = Color(0xFF00E676)
val PrimaryGreenDim = Color(0xFF00B248)
val OnPrimary = Color(0xFF003D1A)

// İkincil: Camgöbeği (detaylar)
val SecondaryTeal = Color(0xFF00BCD4)
val OnSecondary = Color(0xFF001F26)

// Uyarı renkleri
val AlertRed = Color(0xFFFF5252)
val AlertRedDim = Color(0xFFB71C1C)
val AlertOrange = Color(0xFFFF9800)
val AlertYellow = Color(0xFFFFD600)

// İzin türü renk kodları
val ColorCamera = Color(0xFFFF5252)        // Kırmızı — kamera en hassas
val ColorMicrophone = Color(0xFFFF9800)    // Turuncu — mikrofon
val ColorLocation = Color(0xFF00BCD4)      // Camgöbeği — konum
val ColorContacts = Color(0xFFAB47BC)      // Mor — rehber
val ColorCallLog = Color(0xFFEF5350)       // Koyu kırmızı — arama geçmişi
val ColorCalendar = Color(0xFF42A5F5)      // Mavi — takvim
val ColorSms = Color(0xFF66BB6A)           // Yeşil — SMS
val ColorStorage = Color(0xFF8D6E63)       // Kahve — depolama
val ColorGeneric = Color(0xFF78909C)       // Gri — diğer

// Metin renkleri
val TextPrimary = Color(0xFFEEEEEE)
val TextSecondary = Color(0xFF9E9E9E)
val TextDisabled = Color(0xFF616161)
val TextOnDark = Color(0xFFFFFFFF)

// Açık tema (minimal destek — koyu tema öncelikli)
val LightBackground = Color(0xFFF5F5F5)
val LightSurface = Color(0xFFFFFFFF)
val LightPrimary = Color(0xFF00695C)
val OnLightPrimary = Color(0xFFFFFFFF)
